{
    "name": "financial_data_api",
    "description": "Retrieves structured financial statement metrics (revenue, net income, gross margin, EPS, operating cash flow, balance sheet) across recent fiscal years and quarters. Use for quantitative financial analysis.",
    "parameters": {
        "type": "object",
        "properties": {
            "ticker": {
                "type": "string",
                "description": "The stock ticker symbol (e.g. AAPL, MSFT, TSLA)."
            },
            "period": {
                "type": "string",
                "enum": ["annual", "quarter"],
                "default": "annual",
                "description": "Reporting period frequency."
            },
            "limit": {
                "type": "integer",
                "default": 4,
                "description": "Number of periods to return."
            }
        },
        "required": [
            "ticker"
        ]
    }
}